export const metadata = {
  title: "Login — Meridian Monitor",
  description: "Melde dich an um auf dein Meridian Monitor Konto zuzugreifen.",
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
