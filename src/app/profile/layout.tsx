export const metadata = {
  title: "Profil — Meridian Monitor",
  description: "Dein Meridian Monitor Profil und Abo-Status.",
};

export default function ProfileLayout({ children }: { children: React.ReactNode }) {
  return children;
}
